# Base URL for the backend API (FastAPI)
# Example: http://localhost:8000
VITE_API_BASE_URL=http://localhost:8000

# Optional: live updates URL (if backend hosts updates under a different origin).
# If not set, defaults to VITE_API_BASE_URL.
VITE_LIVE_UPDATES_BASE_URL=http://localhost:8000
